# Backend (Hub FyorGyn API) - dependencias de producao
# Estilo ">=" alinhado ao backend_worker/requirements.txt
# Versoes piso espelham o ambiente local validado (pip freeze).

# Web / ASGI
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
pydantic>=2.12.5
python-multipart>=0.0.21

# Banco de dados (Postgres / Supabase)
SQLAlchemy>=2.0.45
psycopg2-binary>=2.9.11

# Cache (Redis / Upstash via REDIS_URL com fallback in-memory)
redis>=8.0.0

# Auth / criptografia
PyJWT>=2.11.0
bcrypt>=5.0.0
cryptography>=46.0.3

# IA (Gemini) - import lazy em services/gemini_client.py
google-genai>=1.65.0

# PDF / OCR (comprovante em services/pdf_comprovante.py; rotacao em services/pdf_rotator.py)
# Nota: pytesseract exige o binario Tesseract no SO (ver DEPLOY.md).
PyMuPDF>=1.26.7
Pillow>=12.1.0
pytesseract>=0.3.13

# Planilhas / dados
pandas>=2.3.3
openpyxl>=3.1.5

# HTTP / config auxiliares
requests>=2.32.5
httpx>=0.28.1
PyYAML>=6.0.3

# Env
python-dotenv>=1.2.1
